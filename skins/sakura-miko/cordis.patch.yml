# sakura-miko skin bundle patch: inserts its dsh.client row into the web plugin roster.
# Mutual exclusion between skins is managed by the skin-center's dsh-skin use
# (home-layer managed rows), so this bundle only contributes the entry id.
- insert:
    - id: ui-skin-sakura-miko
      name: '@dsh-external/dsh-client-ui-skin-sakura-miko'
